{
  "prismic": {
    "apiEndpoint": {
      "doc": "The URL endpoint as provided by Prismic",
      "format": "String",
      "default": "https://bashkim-com.cdn.prismic.io/api/v2"
    },
    "preview": {
      "maxAge": {
        "doc": "The maximum amount of time (in minutes) a preview cookie lasts",
        "format": "Number",
        "default": 30
      }
    }
  }
}
