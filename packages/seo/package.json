{
  "name": "@bashkim-com/seo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "yarn clean",
    "build": "tsc",
    "clean": "rm -rf build/",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "@vendia/serverless-express": "^4.3.3",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.8",
    "typescript": "^4.0.3"
  }
}
