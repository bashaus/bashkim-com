{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "codegen": {
      "inputs": ["generated/prismic/**", "src/**/*.gql"],
      "outputs": ["./dist/**"]
    }
  }
}
