import Box from "@mui/material/Box";
import { styled } from "@mui/material/styles";

export const Icon = styled(Box)(() => ({
  borderRadius: "50%",
  width: 128,
  height: 128,
  overflow: "hidden",
}));
