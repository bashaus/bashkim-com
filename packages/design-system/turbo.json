{
  "$schema": "https://v2-9-6.turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "storybook:build": {
      "dependsOn": ["^compile"],
      "outputs": ["storybook-static/"]
    },

    "test": {
      "dependsOn": ["^compile"]
    }
  }
}
