{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "storybook:build": {
      "dependsOn": ["^compile"],
      "outputs": ["storybook-static/"]
    },

    "test": {
      "dependsOn": ["codegen"]
    }
  }
}
