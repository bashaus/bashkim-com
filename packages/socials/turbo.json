{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["codegen"],
      "env": ["GITHUB_TOKEN"]
    },

    "codegen": {
      "env": ["GITHUB_TOKEN"]
    }
  }
}
