import { Footer } from ".";

export default {
  component: Footer,
  title: "Components/Footer",
};

const Template = ({ ...args }) => <Footer {...args} />;

export const Render = Template.bind({});
Render.args = {};
