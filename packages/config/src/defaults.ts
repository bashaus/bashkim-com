import googleAnalytics from "./defaults/googleAnalytics";
import next from "./defaults/next";
import prismic from "./defaults/prismic";
import sentry from "./defaults/sentry";
import server from "./defaults/server";
import sitemap from "./defaults/sitemap";

export const defaultsConfig = {
  googleAnalytics,
  next,
  prismic,
  sentry,
  server,
  sitemap,
};
