# @type string
# The base URL to run test:e2e
PLAYWRIGHT_BASE_URL="http://localhost:3000"

# @type boolean
# Whether or not to use headless mode
PLAYWRIGHT_HEADLESS="{{#if playwrightHeadless}}true{{else}}false{{/if}}"
